# Common Redis image and initContainers configuration shared by all presets

global:
  security:
    allowInsecureImages: true

image:
  repository: bitnamilegacy/redis
  tag: 7.2.5

auth:
  enabled: true
  ## password will be autogenerated and stored in a Secret
