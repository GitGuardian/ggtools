architecture: standalone

image:
  repository: docker.io/bitnamilegacy/redis
auth:
  enabled: true

master:
  persistence:
    enabled: true
    size: 100Gi
  resources:
    requests:
      cpu: "8000m"
      memory: "16Gi"
    limits:
      cpu: "8000m"
      memory: "16Gi"

